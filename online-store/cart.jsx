<div className='content page'>
    <div>
        <h1>This is where the catalog is going to be.</h1>
    </div>

    <div className="filters">
        {/* create an all button,
            when clicked call a clearFilter fuction,
            the function set the selectedCategory to be ""}
        */}
        {categories.map(cat => <button onClick={() =>onCategorySelected(cat)} className='btn-sm btn'
    </div>

    <div>
            {catalog
                .filter(prod => !selectedCategory
                .map(prod => <Product data=)
                )
            }
    
    
    
    </div>




    </div>